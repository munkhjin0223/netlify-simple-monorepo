{
  "name": "simple-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:bike-store": "cd bike-store && npm i && npm run build",
    "build:car-store": "cd car-store && npm i && npm run build"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
